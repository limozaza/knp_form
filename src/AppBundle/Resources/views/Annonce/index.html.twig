{% extends '::base.html.twig' %}
{% block body %}
    <h3>Listes des annonces</h3>
    {% for annonce in annonces %}
        <p>
            {{ annonce.id }}  ::
            <a href="{{ path('annonce',{'id' : annonce.id}) }}">{{ annonce.name }}</a>
             :: <strong>{{ annonce.categories|length }}</strong>
        </p>
    {% endfor %}
{% endblock %}
