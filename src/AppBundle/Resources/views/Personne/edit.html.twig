{% extends 'AppBundle:Personne:formLayout.html.twig' %}
{% block body %}
    <h3>Editer personne</h3>
    <div class="row">
        <div class="col-xs-12">
            <h1>Editer Personne</h1>
            {{ include('AppBundle:Personne:_form.html.twig', {'buttonText': 'Editer'}) }}
        </div>
    </div>
{% endblock %}

